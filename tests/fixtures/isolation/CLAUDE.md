REPO ROOT CONTEXT — this is the developer's repo-level CLAUDE.md.
It contains secrets, repo conventions, and unrelated instructions.
It must NEVER end up in a deployed managed agent's system prompt.
SECRET_MARKER_DO_NOT_LEAK
