# Acme app — repo instructions (LOCAL ONLY)

This is the repo-level CLAUDE.md for a real project. It holds dev conventions,
internal URLs, and notes that must **never** be shipped to a cloud agent.

agentlift never reads this file. It only deploys what's under `.managed-agents/`.
INTERNAL_ONLY_MARKER
